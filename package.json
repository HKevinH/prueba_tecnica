{
  "name": "agentemotor-cartera",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "src/backend",
    "src/frontend"
  ],
  "scripts": {
    "dev": "concurrently -n backend,frontend -c blue,green \"npm run dev --workspace=src/backend\" \"npm run dev --workspace=src/frontend\"",
    "db:setup": "npm run db:generate --workspace=src/backend && npm run db:push --workspace=src/backend && npm run db:seed --workspace=src/backend",
    "test": "jest --config jest.config.js"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^20.11.0",
    "concurrently": "^8.2.2",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2",
    "typescript": "^5.3.3"
  }
}
